{
  "name": "dune-ops-observability-addon",
  "version": "0.4.1",
  "private": true,
  "description": "Read-only operations addon for Dune Docker Console. No runtime or build dependencies by design — the addon UI (web/) ships as plain HTML/CSS/JS with no bundler.",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {}
}
