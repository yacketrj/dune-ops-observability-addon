name: CI Gate

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  gate:
    runs-on: ubuntu-latest
    needs:
      - validate
      - pre-commit
      - semgrep
      - gitleaks
      - trivy
    steps:
      - run: echo "All checks passed"
